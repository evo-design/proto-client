"""MCP server exposing Proto Bio capabilities to AI agents.

Install with ``pip install proto-client[mcp]``, then run::

    python -m proto_client.mcp              # stdio (Claude Desktop/Code)
    python -m proto_client.mcp --transport http --port 9300  # HTTP (remote access)
"""
